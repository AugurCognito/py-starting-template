---
description: Write an implementation plan before a long task
---

Write a plan for: $ARGUMENTS

1. Read the relevant source first — the code is authoritative.
2. Write the plan to `docs/plans/YYYY-MM-DD-<slug>.md` (absolute date): goal, non-goals, files to touch, test plan (which mirrors in `tests/` change), risks.
3. Keep it under a page. A plan that needs more is two tasks.
